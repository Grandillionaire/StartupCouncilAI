/**
 * Test Architecture Page
 * Route: /test-architecture
 *
 * Purpose: Verify new Zustand + Service + Hook architecture works perfectly
 * before migrating the main CouncilChat component
 */

import TestDebate from '@/components/TestDebate';

export default function TestArchitecturePage() {
  return <TestDebate />;
}
